# Linked Data REPL roadmap

This is a living experimental backlog. Status means project-local evidence only; it is not a production commitment or approval for future live access. Material work selected for execution belongs in the [active task queue](tasks/README.md), where status and handoff evidence can remain durable across Codex tasks.

## Implemented foundations

| Slice | Hypothesis | Acceptance evidence | Status |
| --- | --- | --- | --- |
| User-owned clean-room Linked Science runtime | A project-registered clean-room CodeAct MCP can expose the Linked Science facade with explicit roots, normal module-scoped dependency resolution, RLM discovery, broker-owned PEEK orientation, persistent handles, and honest kernel-reset behavior. | Offline tests and real-server JSON-RPC check; fresh Local Desktop task observed the exact three tools, cross-call persistence, declared dependency resolutions, bounded synthetic query/provenance, RLM registration, PEEK survival, facade re-bootstrap, and stale old handles. | Implemented and validated locally on 2026-08-20. See [runtime discovery](agent/runtime-discovery.md) and the [bootstrap task record](tasks/linked-science-cleanroom-bootstrap.md). |
| Open-world mediated federation | Consumer-owned Communica can dereference dynamically discovered anonymous HTTP/HTTPS RDF resources and SPARQL services while every request remains visible to a token-bound parent standard-Fetch mediator. | Actual standard-Fetch loopback fixture; header/parser/budget/cancellation tests; two-source, two-service, and complete RDF-document Communica fixtures with native handles and complete lineage; no new MCP/facade routes. | Protocol 3.0.0 implemented and validated offline on 2026-08-21. The first neutral live preflight remains an immutable failure from the removed custom DNS connector; no baseline ran. A Desktop restart and separately authorized new preflight remain required. See the [machine receipt](../artifacts/experiment-results/2026-08-21-neutral-mediated-traversal-preflight.json) and [current architecture](architecture/broker-owned-live-operations.md). |
| Persistent synthetic session | A worker can retain materialized local bindings behind symbolic handles across REPL calls. | Offline tests plus a persistent-REPL demonstration of materialize, profile, page, derive, and reset recognition. | Implemented and validated. |
| Historical fixed-profile transports | Earlier endpoint-pinned guards produced bounded live-table and query-to-handle evidence with attempt receipts. | Preserved tests, experiment dossiers, and immutable result receipts. | Superseded for production traversal by the open-world mediator; retained only to interpret prior experiments. |
| Presentation handoff | A coordinator can receive a display-safe bounded view without raw result material. | Offline display-model tests and persistent-REPL proof from a retained live handle. | Implemented for typed tables only. |
| Guarded evidence acquisition and symbolic orientation | A worker can retain a messy approved source or failed attempt, content-sniff metadata disagreement, and preserve reusable PEEK-style orientation without raw context. | Offline success, metadata-mismatch, failure-retention, preflight refusal, stable-map, eviction, and raw-query-exclusion tests. | Initial slice implemented and validated; live source profiles remain separately approval-bound. |
| WikiPathways-to-ChEBI traversal | A worker can retrieve ChEBI identifier links from bounded WikiPathways results and enrich one validated accession without pretending ChEBI is a SPARQL service or downloading its full ontology. | WikiPathways profile query/refusal tests; exact official ChEBI compound-profile acquisition and path-substitution refusal tests. | Implemented locally; live WikiPathways and ChEBI reads remain task-approval-bound. |

## Next experimental slices

| Slice | Hypothesis | Acceptance evidence | Status |
| --- | --- | --- | --- |
| UniProt competency-question evaluation | The official UniProt SPARQL example catalog can serve as an externally authored held-out corpus for evaluating the complete clean-room agent loop without leaking target queries to workers. | Versioned split worker/evaluator corpus; leakage audit; fresh-task chronological traces; semantic-reference comparison; repeated trials across staged service, entity, ontology, annotation, aggregation, and later federation tiers. | Offline runtime, worker manifest, private evaluator bundle, and post-restart ownership checks are complete. Next: one bounded live preflight, then one frozen fresh-worker baseline and private evaluation. See the [manifest task](tasks/uniprot-competency-manifest.md) and [methodology dossier](experiments/uniprot-competency-question-evaluation.md). |
| Blind clean-worker map evaluation | A fresh worker can use repository-local map/session guidance rather than prior conversation or raw rows. | Fresh-task trace proves persistent-REPL preflight before any satisfiable source analysis, an execution-backed conclusion, map-first orientation, cited bounded handle reuse/derivation/display, provenance/budget reporting, and no external access. | Validated for two constrained local scenarios. The 2026-08-20 [multi-turn sanity check](tasks/context-recovery-sanity-check.md) passed all seven criteria with preserved ambiguity, genuine cross-turn derivation, and stale PEEK references after an actual kernel reset. Prior open runs remain failed/inconclusive, and open-ended navigation or external data remain untested. See the [protocol and recorded runs](experiments/clean-worker-map-evaluation.md). |
| Legacy affordance catalog | A worker can use ontology/documentation-derived prefix and motif affordances to plan a bounded query before endpoint exploration. | Catalog lookup/plan/validation tests. | Retained as compatibility/experiment record; not agent-facing. Replaced by the minimal evidence manifest. See [protocol](experiments/affordance-catalog.md). |
| Minimal evidence manifest | A worker receives authoritative resource locations and policy references without a hidden planner. | Manifest validation; fresh-worker use of ontology/examples/named-graph resources on natural questions. | Implemented locally; worker use remains pending. The open-goal workers retrieved the ontology and used the approved profile, but did not visibly read the manifest or official examples. |
| UniProt schema discovery | A worker can acquire a primary schema document, retain a compact provenance-bearing view, and plan from it without probing data. | Persistent-REPL two-call trace with one pinned documentation request, compact schema view, cited terms/plan, and no SPARQL transport. | Validated once on 2026-08-15 with one 501,448-byte canonical schema document. It is documentation evidence only; data-level navigation remains untested. See [protocol](experiments/uniprot-schema-discovery.md). |
| Prior grounding and tool-surface evaluation | Useful pretrained knowledge becomes explicitly reconciled hypotheses, while trajectory friction is attributed to methodology, tool surface, environment, source, or mixed causes. | Fresh-worker, same-scenario comparison across baseline, grounding method, and grounding method plus pack; validated compact receipts and chronological tool traces. | Recorded and superseded as the primary direction — local task was trivial and baseline guidance leaked. See [dossier](experiments/prior-grounding-tool-surface-evaluation.md). |
| Codex-goal-compatible evidence state | Minimal tool-enforced invariants plus project-local evidence/session state help a worker pursue an existing Codex goal without a brittle narration script or a competing goal lifecycle. | Local evidence/session prototype attached to a Codex goal; fresh-worker comparison; source-failure, stale-prior, ambiguity, empty-result, large-handle, and reset perturbations. | Partially implemented: guarded acquisition, evidence/failure handles, and bounded symbolic orientation map are tested; fresh-worker and remaining perturbation evaluation is pending. See [dossier](experiments/goal-loop-state-graph.md). |
| Streamed large-result export | A retained handle can stream to a controlled local artifact without entering coordinator context. Start with a synthetic 10,000-row result; any live trial needs separate approval. | Deterministic synthetic 10k test; artifact path/format/schema/count/hash/provenance/lineage receipt; no-overwrite behavior; bounded reopening check. | Planned — no exporter or generated dataset exists. See [protocol](experiments/large-result-export.md). |
| Artifact re-open and derived analysis | An exported artifact can be reopened as a distinct local source and yield bounded derived handles without treating it as the original resident result. | Deterministic reopen, schema/hash verification, lineage link, bounded page/aggregate tests. | Planned. |
| Reset and rematerialization | A reset can be recognized honestly and a handle rematerialized only through its authorized source path. | Tests for invalidation, missing-handle reporting, explicit rematerialization receipt, and refusal when authorization/source is absent. | Planned. |
| Richer bounded presentation | Tables can gain additional typed bounded views, while future charts never consume whole results. | Scalar/budget/provenance tests per kind and persistent-REPL proof using a derived handle or explicit page. | Planned — charts are not implemented. |
| Live mediated federation baseline | A fresh worker can navigate public Linked Data without case-specific endpoint hints while behavior and aggregate resources remain bounded. | Separately authorized fresh-worker trace, complete per-exchange lineage, evaluator-private scoring, and no prompt tuning before the first result is recorded. | Blocked on activation evidence: the custom DNS/TLS path that failed the first preflight has been removed and replaced offline by protocol 3.0.0 standard Fetch. Restart Desktop, then obtain separate authorization for one neutral preflight before any baseline. |
| Durable clean-room dataset persistence | An explicitly saved RDF dataset can survive `js_reset` and reload as a fresh epoch-bound handle while the child retains no ambient filesystem authority. | Versioned host-mediated save/load/list/remove contract; canonical dataset serialization and content hash; confined atomic storage with quotas and validation; provenance/lineage metadata; synthetic save-reset-reload, stale-handle, corruption, retention, and authorization tests. | [Deferred task](tasks/durable-dataset-persistence.md) — begin only after the bounded UniProt preflight and first frozen baseline are assessed. |
| Wiki memory and continual-learning distillation | Evidence-backed teaching trajectories can yield reviewed, versioned lessons that a later skill retrieves selectively without importing whole transcripts, stale claims, or held-out answers. | Layered evidence-to-lesson-to-wiki pipeline; typed memory model and review policy; bounded skill attachment/retrieval; conflict and expiry handling; cross-endpoint transfer evaluation with leakage controls. | [Deferred research task](tasks/wiki-memory-continual-learning.md) — not a prerequisite for the current baseline and independent of durable RDF artifact persistence, though both may cross-reference provenance. |

## Operating boundary

An in-memory handle, a bounded display model, and a future local export are different capabilities. Context maps retain only compact metadata. Any write, export, or live trial requires its own explicit authorization. Dynamic destinations discovered during an authorized traversal are validated by the mediator rather than preapproved as an endpoint list.
