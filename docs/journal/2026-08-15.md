# Work log — 2026-08-15

## Purpose

Record the project's experimental evolution and the evidence behind agent-behavior claims. This log complements project task history; it does not replace generated receipts or Git provenance.

## Completed today

- Established the local Git repository and baseline setup documentation.
- Added guarded, read-only Communica access for the named Identifiers.org SPARQL endpoint, plus local synthetic smoke coverage.
- Implemented persistent REPL result handles, compact context maps, bounded table-display handoff, and guarded live-table demonstrations.
- Recorded and committed a two-turn coordinator/worker context-map experiment. It demonstrated bounded steering but was intentionally classified as scripted rather than proof of general navigation.
- Added `AGENTS.md`, the roadmap, experiment protocols, and the project-local Linked Data REPL skill.
- Added a clean-worker evaluation protocol and attempted fresh-worker local synthetic checks. They are now classified as **informative failed/inconclusive preflights**, not as retained-state evidence: an earlier trajectory lacked an actual persistent-JS-REPL state receipt, and the newest trace avoided unsupported state claims but answered from repository source. CLI/terminal output and repository inspection cannot establish materialization, reuse, recovery, or an execution-backed evaluation conclusion.
- Ran the first qualifying constrained clean-worker evaluation after tightening the task-entry preflight. The fresh worker made actual persistent-JS-REPL calls, materialized one local 120-row synthetic handle, derived and displayed a bounded two-category result, then followed a coordinator-selected second action by deriving a five-row membership page without rerunning the source. A controlled REPL reset removed all handles, and the worker correctly reported that no prior result could remain claimed without explicit rematerialization. See the [evaluation dossier](../experiments/clean-worker-map-evaluation.md).
- Added the explicitly approved UniProt/Rhea/Wikidata federation profile: exact endpoint pins, read-only `SELECT`, `LIMIT 1-10`, no redirects/retries, and `SERVICE` only to the named Rhea and Wikidata endpoints. This is local policy/test coverage only; no live federated query has run yet.
- Added a source-backed UniProt/Rhea/Wikidata affordance catalog. It turns selected prefixes and official example motifs into bounded, non-executing plans before query construction, while retaining only compact planning metadata in the context map. It is not yet a clean-worker or live federation result.
- Completed the first live UniProt schema-document trial. A persistent REPL made one guarded request to the pinned rendered RDF-schema page, retained the 501,448-byte HTML only in memory, derived a compact `up:Protein → up:annotation → up:catalyticActivity` documentation view, and reused it in a second call without re-fetching or contacting a data endpoint. See the [schema-discovery protocol](../experiments/uniprot-schema-discovery.md).
- Added a prior-grounding and tool-surface evaluation scaffold. It treats pretrained knowledge as candidate claims that require source reconciliation, compares three fresh-worker arms, and records whether a trajectory issue is methodological, a missing REPL affordance, environmental, source-related, or mixed.
- Ran its first local three-arm scenario after fixing the receipt validator to require an explicit terminal outcome. Each fresh worker used a persistent REPL, locally grounded the two-edge path, and made one bounded `ASK` returning true. The pack arm corrected an initially assumed store count after inspection. This confirms the basic mechanics only: baseline guidance leakage and a trivial task prevent any claim that a method or pack improved trajectories.
- Audited a fresh real-UniProt worker after its approved documentation fetch failed. It stopped before a data query, which was correct, but also preselected schema terms before retrieval, reported an unbound session, prescribed a future `SELECT` without schema evidence, and bypassed the receipt helper. This changed the project direction: replace the narrative checklist with minimal invariants and a Codex-goal-compatible Linked Data evidence/session layer—not a competing goal graph. See [the decision dossier](../experiments/goal-loop-state-graph.md).

## Provenance checkpoints

- `1b69b8b` — guarded Linked Data REPL experiment
- `f89315c` — coordinator-steered context-map experiment
- `4342068` — REPL result handles and roadmap
- `7b92179` — clean-worker map evaluation protocol

## Current interpretation

The project has implementation tests, controlled persistent-REPL demonstrations, and one qualifying constrained clean-worker evaluation. The first clean worker did begin with a persistent-REPL preflight, reused a retained handle under coordinator steering, and responded honestly to reset. It does not yet establish that a fresh worker will form good trajectories under an open-ended multi-turn goal, navigate a semantic endpoint, or manage large exported artifacts.

## Next evidence to obtain

1. Build the smallest Codex-goal-compatible evidence/session layer, starting with session/source/schema/claim states and verified transitions.
2. Compare it with the current checklist on an open local goal and perturb the source, prior, result, handle, and reset states.
3. Verify the fresh-worker UniProt documentation transport separately before retrying real schema-to-query navigation.
4. Then return to large-result export as an evidence-session/handle capability rather than a standalone feature.
