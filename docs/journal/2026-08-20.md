# 2026-08-20

- Integrated the committed Linked Science CodeAct facade into the saved checkout and replaced the obsolete bundled-REPL configuration with the user-owned `cleanroom_node_repl` registration.
- Added an explicit clean-room bootstrap that validates the Linked Science project and module roots, resolves every declared dependency from the project module tree, registers bounded runtime discovery in `nodeRepl.rlm`, and delegates runtime orientation to broker-owned `nodeRepl.peek` rather than constructing a competing cache.
- Preserved the former restricted network profile as disabled reference configuration. No live endpoint, network probe, package change, global configuration change, or clean-room repository edit occurred.
- Verified the actual clean-room JSON-RPC server offline and in a genuinely fresh Local Desktop task. Both checks established cross-call persistence, one-time facade bootstrap, bounded local-synthetic Communica operation, RLM discovery, PEEK survival across kernel reset, re-bootstrap, and stale old handles.
