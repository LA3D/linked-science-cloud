{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://linked-science.local/schema/uniprot-competency-worker-manifest-v2.json",
  "title": "Worker-visible UniProt competency manifest",
  "type": "object",
  "additionalProperties": false,
  "required": ["format", "status", "version", "corpusSnapshotDigest", "accessDate", "cases"],
  "properties": {
    "format": { "const": "linked-science-competency-worker/v2" },
    "status": { "enum": ["draft", "ready"] },
    "version": { "type": "string", "minLength": 1 },
    "corpusSnapshotDigest": { "type": "string", "pattern": "^(pending|[a-f0-9]{64})$" },
    "accessDate": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$" },
    "cases": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "tier", "question", "resources", "bounds", "secondTurn"],
        "properties": {
          "id": { "type": "string", "pattern": "^uq-[a-f0-9]{12}$" },
          "tier": { "type": "integer", "minimum": 0, "maximum": 5 },
          "question": { "type": "string", "minLength": 1, "maxLength": 1000 },
          "resources": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["role", "access", "authorizationRequired"],
              "properties": {
                "role": { "type": "string", "minLength": 1 },
                "access": { "const": "mediated-public-https-linked-data" },
                "authorizationRequired": { "const": true }
              }
            }
          },
          "bounds": {
            "type": "object",
            "additionalProperties": false,
            "required": ["maxHops", "maxFanOut", "maxConcurrency", "maxRows", "maxCells", "maxResponseBytes", "maxTotalBytes", "maxDurationMs", "maxResultItems", "maxQueryChars"],
            "properties": {
              "maxHops": { "type": "integer", "minimum": 1, "maximum": 32 },
              "maxFanOut": { "type": "integer", "minimum": 1, "maximum": 16 },
              "maxConcurrency": { "type": "integer", "minimum": 1, "maximum": 6 },
              "maxRows": { "type": "integer", "minimum": 1, "maximum": 10 },
              "maxCells": { "type": "integer", "minimum": 1, "maximum": 100 },
              "maxResponseBytes": { "type": "integer", "minimum": 1, "maximum": 4000000 },
              "maxTotalBytes": { "type": "integer", "minimum": 1, "maximum": 20000000 },
              "maxDurationMs": { "type": "integer", "minimum": 1, "maximum": 120000 },
              "maxResultItems": { "type": "integer", "minimum": 1, "maximum": 5000 },
              "maxQueryChars": { "type": "integer", "minimum": 1, "maximum": 16384 }
            }
          },
          "secondTurn": { "const": "question-neutral-retained-handle-reuse" }
        }
      }
    }
  }
}
