# Independent recovery evidence review

2026-09-19. **Inconclusive:** a bounded search of three saved candidate groups found no independent scientific episode admissible under the existing corpus contract. The maintainer was listed in the current skill catalog, read and used by the current assistant. This was not a fresh-task or blinded model evaluation.

The [review record](../../artifacts/wiki-learning/reviews/independent-recovery-20260919/review.json) preserves selected input hashes, comparison decisions and omissions. [Corpus validation](../../artifacts/wiki-learning/reviews/independent-recovery-20260919/corpus-validation.json) confirms one eligible episode: the original seed. [Wiki validation](../../artifacts/wiki-learning/reviews/independent-recovery-20260919/wiki-validation.json) confirms the existing revision and citation integrity.

| Independent candidate | Observed boundary | Decision |
| --- | --- | --- |
| August 25 UniProt baseline | Trace-derived summary reports zero bindings, `LS_COLUMNS` and a later duration-limit error. Durable raw correlation is incomplete. | No demonstrated in-scope recovery or admissible corpus unit; neither support nor contradiction. |
| September 8 orientation transfer probe | Saved orientation metadata, zero queries and an epoch-scoped reference. | Runtime diagnostic, not independent scientific recovery evidence. |
| September 8 synthetic library reset | Saved raw output reports `LS_STALE_WORKSPACE` and an empty fresh inventory. | Outside scientific corpus scope, and reset is already excluded by the proposed guidance. |

The candidate already requires a completed query, a valid handle in the same epoch and a presentation-bound error. A reset failure therefore does not falsify it. An empty scientific result with another error also does not demonstrate failure of bounded-page recovery. Reusing the original episode would not supply independence.

No proposal was created and no wiki revision was applied. Both patterns remain proposed. Hashes protect record integrity; they do not upgrade a summary into raw operation evidence. The review is registered as inconclusive, not a passed transfer experiment. This bounded sample does not establish that no suitable episode exists elsewhere.

Next: capture an independent scientific comparison with directly saved completion, presentation failure and recovery exchanges. Include a separately labeled release/reset boundary control; do not score it as an in-scope counterexample. Validate its scientific corpus units before revising the candidate. No new source acquisition or live experiment occurred during this review.

Delivery starts at `bec6f25c84b6d75e8e60827bad989ca5c1d93e89` on `codex/wiki-independent-review` in the authoritative `codex-repl` checkout. The unrelated project MCP configuration and structure-viewer artifact remain excluded. Required verification and the integrated commit are reported in the completion response.

Verification: full `npm test` passed 248/249 initially, with only the registry's expected run/partial counts needing the new review. After updating those counts, the focused registry suite passed 3/3; the remaining 246 tests had passed in the full run. Smoke, registry validation (63 runs), seven selected input hashes, unchanged wiki HEAD, relative Markdown links and whitespace checks passed. No runtime code or skill changed.
