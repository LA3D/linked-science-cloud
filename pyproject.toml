[project]
name = "science-tools-rdkit-experiment"
version = "0.0.0"
description = "Isolated RDKit dependency contract for the science-tools 2D chemistry experiment."
requires-python = ">=3.11,<3.14"
dependencies = [
  "rdkit==2026.3.5",
]

[tool.uv]
package = false

