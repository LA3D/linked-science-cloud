# PubChem retained evidence and WikiSkill maintenance

The originating scientific task captured still-resident objects on September 21, 2026, after direct user authorization. Original source retrievals occurred earlier the same day. No new scientific network request or reset was performed for this capture.

`runtime-capture.json` and its supplement preserve exact new MCP arguments and results. Normalized `exchange-*.json` wrappers do not invent original JSON-RPC request IDs. The first capture objective used inspected text; its projected question is extracted from that observed text. Later outputs are JSON. No capture block reports truncation. Endpoint crosswalk/reference pages are deliberately partial displays, although their provenance records complete submitted-query results. Handles record capture-time identity, not durable or future residency.

The actual local schema-count profile supplies the joined query witness. Its retained-state checks confirm the schema count and labels for the enumerated SIO/CHEMINF terms. The other capture events preserve original endpoint receipts, annotations, selective-import summaries, assay qualifiers, and source context. `observed-outcomes.json` is a projection of those actual outputs, not an independent biological validation.

Original parser-error and timeout envelopes were not recovered; their recovery narrative remains retrospective. No global absence, complete import closure, universal transfer, causal benefit, full external graph residency, isoform alignment or comparable potency ranking is established. Source annotations and endpoint usage remain separate evidence.

Four proposed entries were applied: a general ontology methodology supported by the PubChem example; schema meaning versus endpoint use; cross-source protein/assay interpretation; and a dated source snapshot. Existing paging recovery guidance was not duplicated. Earlier retrospective drafts remain labeled historical.

Verification: proposal inspection, current wiki validation, search retrieval of the general method, smoke test, README relative-link check, and whitespace checks passed. The required full test run reports 287 passed and one pre-existing checkout-path assertion failure in `test/cleanroom-linked-science-bootstrap.test.mjs:69` (expects `codex-repl`; actual checkout is `agents/linked-science-cloud`). Machine configuration was preserved. No runtime or active skill files changed.

Repository work began at b2946cbd9d52abe8646ab537769aef904ff7001c on branch codex/pubchem-wiki-methodology. The README leads with MCP-provided symbolic context, distinguishes user-reported client testing from repository verification, and cites the primary RLM and WikiSkill papers. This maintenance does not claim that optional recursion or automatic skill evolution is active.
