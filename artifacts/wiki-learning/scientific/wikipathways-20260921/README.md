# WikiPathways TP53 retained-evidence capture

The user authorized incorporating this trajectory into WikiSkill on September 21, 2026. The original public source and endpoint reads occurred September 20. This is a direct inspection of still-resident handles, not a reconstructed original tool transcript or a fresh endpoint evaluation.

Capture exchange 5 declares the capture objective; exchanges 1–4 follow it in capture order. Exchange 2 runs a local ontology audit. Its initial row projection mistakenly assumes variable objects as keys; exchange 3 corrects the projection without another query and checks native iteration counts against retained profiles. This projection defect is preserved, not offered as a general recovery pattern.

Saved artifacts contain bounded source metadata, complete-result profiles and original remote receipts, compact counts, selected examples, and exact membership/involvement query text. Complete RDF documents and remote row sets remain behind handles. These artifacts cannot restore a lost session. Observable events are explicitly projected from actual capture tool results; they are not fabricated historical host events. The local schema query is the joined query witness; remote profiles separately support the dated counts.

The source reports 92 human-pathway memberships, 265 interaction IRIs in 66 pathways, and 170 endpoint/participant nodes. Mapping coverage is 141 UniProt, 141 Ensembl, and 61 Wikidata nodes. These counts do not establish unique mechanisms or species-pure mappings. The shared TP53 symbol node returned human and dog-style Ensembl identifiers despite a human-pathway filter. Exact named-graph origins and external destinations were not independently validated.

Provenance for some retained queries reports a result-item budget of 500 despite completed counts greater than 500. The receipts preserve this discrepancy; this maintenance does not establish runtime budget enforcement correctness.

The skill snapshot records maintenance-time files, not historical skill loading. No independent transfer test, no-memory comparison, causal benefit, automatic review promotion, or biological validation is claimed. Separate subagent tag evidence is stored in the adjacent wikipathways-tags-20260921 directory.
